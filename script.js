const root = document.getElementById('root');

const headerElement = document.createElement('h1');
const taskBar = document.createElement('input');

headerElement.innerText = 'Hello'

root.append(headerElement)
root.append(taskBar)